var tags = require('./tags.json');
var HashMap = require('hashmap'); //#include <hashmap.h>
console.log("Hola!");
var map = new HashMap();
map.set("some_key", "some value");
console.log(map.get("some_key"));
var arr = Array(2,4,1,4,1);
console.log(arr[3]);

if (tags[dict].hasOwnProperty("booking"))
{
    console.log('match!', tags.[dict][wrd]);
}
else
{
    console.log("What The Actual Fuck?");
}

// DatteBayo
